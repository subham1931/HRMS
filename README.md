# HRMS Frontend

## Local setup

1. Install dependencies:

```bash
npm install
```

2. Start app:

```bash
npm run dev
```

## Data persistence

- The app now persists data in browser localStorage only.
- Keys used:
  - `hrms_employees`
  - `hrms_attendance`
